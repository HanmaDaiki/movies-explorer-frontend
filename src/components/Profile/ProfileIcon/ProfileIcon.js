import React from 'react';
import profileIcon from '../../../images/icon-profile.svg';
import './ProfileIcon.css'

const ProfileIcon = () => {
  return (
    <img className='profile__icon' src={profileIcon} />
  );
}

export default ProfileIcon;
